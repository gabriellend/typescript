// If you don't write fetchTodos = 'FETCH_TODOS', typescript
// will assign indexes to each value like fetchTodos = 0. It 
// doesn't matter, it just has to be unique
export enum ActionTypes {
  fetchTodos
}
